# Copy to .env.local and adjust values
# Leave empty or omit to use Vite dev proxy (avoids CORS). For production, set full backend URL.
# VITE_API_BASE_URL=http://localhost:8080
VITE_APP_TITLE=Task Manager
